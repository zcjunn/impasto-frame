interface:
  display_name: "Impasto Frame"
  short_description: "Natural painterly frames with source-led colour"
  default_prompt: "Use $impasto-frame to rebuild the supplied image as a naturally authored painterly frame with a thumbnail-visible directorial change, identity-preserving focal beauty, redesigned salient props, source-led colour and motivated subject light. Preserve the source temperature unless a named light or spatial owner justifies a shift; avoid automatic warm grading, dirty saturation and mosaic relief."
